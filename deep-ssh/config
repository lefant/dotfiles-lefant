host *
     HashKnownHosts no
     NoHostAuthenticationForLocalhost yes
     ControlMaster auto
     ControlPath ~/.ssh/sock/%r@%h:%p
     VisualHostKey yes
     #ProxyCommand monkeysphere ssh-proxycommand %h %p

host localhost
     StrictHostKeyChecking no
     UserKnownHostsFile /dev/null

host *.local
     AddressFamily inet
     CheckHostIP no

host *.42.lefant.net
     AddressFamily inet6

host sh.lefant.net
     user e
     AddressFamily inet6

host hip* hop* hep* al* wave* skib* mixer* tune*
     user root

host keisanki.net
     port 22007
host heraklit*
     port 22222
host rd01* srv2*
     port 55022
host vpn-* bar-*
     user root


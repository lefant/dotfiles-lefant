host *
     HashKnownHosts no
     NoHostAuthenticationForLocalhost yes

host localhost
     StrictHostKeyChecking no
     UserKnownHostsFile /dev/null

host *.local
     AddressFamily inet

host hip* hop* hep* al* wave*
     user root

host rd01* srv2*
     port 55022

host vpn-* bar-*
     user root
